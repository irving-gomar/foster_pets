<%= simple_form_for(@service) do |f| %>
  <%= f.input :activity, :collection => %w[walking styling medication feeding babysitting] %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
