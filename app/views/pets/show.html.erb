<div class="container main-box mt-4" style="height:300px">
    <div class="row">
      <div class="col-md-6">
        <div class="text-center" style="overflow: hidden; margin: auto">
          <%= image_tag(@pet.photos[0], class: "p-2", style: "width: 100%; height:100%; border-radius: 20px; object-fit:cover; aspect-ratio: 1/1")%>
        </div>
      </div>
      <div class="col-md-6">
        <div class="card-body">
          <h2 class="card-title"><%= @pet.name %></h2>
          <p class="card-text">
            <strong>Breed:</strong> <%= @pet.breed %><br>
            <strong>Description:</strong> <%= @pet.description %><br>
            <strong>Date of Birth:</strong> <%= @pet.date_of_birth %><br>
            <strong>Weight:</strong> <%= @pet.weight %> kg<br>
            <strong>Size:</strong> <%= @pet.size %> cm
          </p>

          <%= link_to edit_pet_path(@pet), class: 'btn btn-primary mr-2' do %>
            Edit
          <% end %>

          <!-- Delete link -->
          <%= link_to pet_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-danger' do %>
            Delete
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
